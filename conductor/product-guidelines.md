# Product Guidelines

## Core Principles
1. **File Size Constraint:** Python code files (`.py`) MUST NOT exceed 300 lines of code. If a file grows beyond this limit, its logic must be refactored and modularized into smaller, focused files.
2. **Version Control:** Git usage is strictly prohibited within the autonomous routines of this project. Agents and scripts must operate without relying on git commands or states.